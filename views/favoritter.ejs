<%- include('partials/header', { bruger, currentPage: 'favoritter', title: 'Mine Favoritter' }) %>

  <!-- Hero Section -->
  <section class="relative text-white min-h-[70vh] flex items-center overflow-hidden" style="background-image: url('/images/hero/dark-2568157_1280.jpg'); background-size: cover; background-position: center; background-repeat: no-repeat; margin-top: -80px; padding-top: 80px;">
    <!-- Dark overlay for better text readability -->
    <div class="absolute inset-0 bg-black bg-opacity-70"></div>
    
    <div class="content-container py-20 relative z-20">
        <div class="text-center">
      <h2 class="text-6xl font-bold text-gold mb-4 drop-shadow-2xl">❤️ Mine Favoritter</h2>
      <p class="text-2xl text-cream drop-shadow-lg">Alle dine markerede rekvisitter samlet ét sted</p>
   </div>
</div>
  </section>

  <!-- Main Content -->
  <main class="min-h-screen py-12 bg-cream">
    <div class="content-container">

      <!-- Products Grid -->
      <% if (produkter.length === 0) { %>
        <div class="text-center py-12 bg-white rounded-lg shadow-lg">
          <p class="text-xl text-gray-600 mb-4">Du har ikke markeret nogle favoritter endnu.</p>
          <a href="/browse" class="inline-block bg-burgundy text-cream px-6 py-3 rounded-lg font-bold hover:bg-opacity-90 transition">
            Browse rekvisitter
          </a>
        </div>
      <% } else { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          <% produkter.forEach(produkt => { %>
            <div class="bg-white border-2 border-gold rounded-lg overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300">
              <!-- Product Image -->
              <div class="h-64 bg-gradient-to-br from-burgundy to-gray-900 flex items-center justify-center border-b-4 border-gold relative overflow-hidden">
                <span class="text-8xl opacity-30">🎭</span>
                <% if (produkt.billede && !produkt.billede.includes('placeholder')) { %>
                  <img src="<%= produkt.billede %>" alt="<%= produkt.navn %>" class="absolute inset-0 w-full h-full object-cover">
                <% } %>
                
                <!-- Favorit badge -->
                <div class="absolute top-4 right-4 bg-white bg-opacity-90 rounded-full p-3 shadow-lg">
                  <span class="text-2xl">❤️</span>
                </div>
              </div>

              <!-- Product Content -->
              <div class="p-6">
                <h3 class="text-2xl font-bold text-burgundy mb-3"><%= produkt.navn %></h3>
                
                <p class="text-sm text-gray-600 mb-2">
                  🎭 <strong><%= produkt.teaterNavn %></strong>
                  <% if (produkt.teaterLokation) { %>
                    <br>📍 <%= produkt.teaterLokation %>
                  <% } %>
                </p>
                
                <% if (produkt.kategori) { %>
                  <div class="flex flex-wrap gap-2 mb-3">
                    <span class="px-2 py-1 bg-gold bg-opacity-20 text-gray-700 text-xs rounded"><%= produkt.kategori.størrelse %></span>
                    <span class="px-2 py-1 bg-burgundy bg-opacity-20 text-gray-700 text-xs rounded"><%= produkt.kategori.æra %></span>
                    <span class="px-2 py-1 bg-blue-100 text-gray-700 text-xs rounded"><%= produkt.kategori.type %></span>
                  </div>
                <% } %>
                
                <p class="text-gray-700 mb-4 leading-relaxed"><%= produkt.beskrivelse %></p>
                
                <% if (produkt.lokation) { %>
                  <p class="text-lg font-semibold text-burgundy mb-4">📍 <%= produkt.lokation %></p>
                <% } %>
                
                <div class="flex gap-2">
                  <a href="/produkt/<%= produkt.id %>" class="flex-1 text-center bg-burgundy text-cream py-3 px-4 rounded font-bold hover:bg-opacity-90 transition">
                    Se Detaljer
                  </a>
                  <form action="/favorit/<%= produkt.id %>" method="POST" class="flex-1">
                    <button type="submit" class="w-full bg-gray-300 text-gray-700 py-3 px-4 rounded font-bold hover:bg-gray-400 transition">
                      Fjern ❌
                    </button>
                  </form>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </div>
  </main>

<%- include('partials/footer', { bruger }) %>
