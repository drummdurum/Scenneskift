# Environment Variables Configuration
# Dette er et eksempel-fil. Kopier denne til .env for lokal udvikling.
# På Railway, tilføj disse variabler under Settings → Variables

# Session Secret - VIGTIGT: Generer en sikker nøgle til produktion!
# Brug: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=teater-hemmelighed-2025

# Port - Railway sætter dette automatisk, men du kan ændre det lokalt
PORT=3000

# Node Environment - Skift til "production" på Railway
NODE_ENV=development
